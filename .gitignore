# .gitignore for ansible-mini-finance project

# --- Terraform Files ---
# Ignore the entire local state/cache directory which contains the large
# downloaded provider binaries (which caused your push failure).
# This directory should be regenerated via 'terraform init'.
.terraform/

# Ignore local state files (contain sensitive data and should be managed remotely)
*.tfstate
*.tfstate.backup

# Ignore the lock file (while useful, it can often be regenerated)
# However, many projects DO track this to ensure provider version stability.
# If you want strict version control, REMOVE the line below.
# For simplicity and to avoid issues, we'll ignore it.
.terraform.lock.hcl

# Ignore the variable definition file (often contains secrets or local paths)
# Only track it if it contains non-sensitive defaults.
terraform.tfvars

# Ignore crash log files
crash.log

# --- Ansible Files ---
# Ignore temporary and log files generated during playbook execution
*.retry
*.log

# --- IDE, OS, and Editor Artifacts ---
# VS Code settings directory (ignore if this is a team project and settings aren't shared)
.vscode/

# Ignore Python virtual environment files (common for Ansible development)
venv/
.venv/

# Operating System and common editor artifacts
.DS_Store
Thumbs.db
*~
*.swp